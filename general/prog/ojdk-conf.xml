<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="ojdk-conf" xreflabel="Konfigurering av JAVA miljøet">
  <?dbhtml filename="ojdk-conf.html"?>


  <title>Konfigurering av JAVA miljøet</title>

  <sect2 id="java-profile">
    <title>Sette opp miljøet</title>

    <para>
      Etter at pakkeinstallasjonen er fullført, er neste trinn å sørge for at 
      systemet kan finne filene på riktig måte. Hvis du konfigurerer 
      innloggingsskriptene dine som anbefalt i <ulink
      url="&blfs-svn;/postlfs/profile.html">Oppstartsfilene til Bash 
      Skallet</ulink>, oppdater miljøet ved å opprette
      <filename>openjdk.sh</filename> skriptet, som &root; bruker:
    </para>

<screen role="root"><userinput>cat &gt; /etc/profile.d/openjdk.sh &lt;&lt; "EOF"
<literal># Begin /etc/profile.d/openjdk.sh

# Set JAVA_HOME directory
JAVA_HOME=/opt/jdk

# Adjust PATH
pathappend $JAVA_HOME/bin

# Auto Java CLASSPATH: Copy jar files to, or create symlinks in, the
# /usr/share/java directory.

AUTO_CLASSPATH_DIR=/usr/share/java

pathprepend . CLASSPATH

for dir in `find ${AUTO_CLASSPATH_DIR} -type d 2>/dev/null`; do
    pathappend $dir CLASSPATH
done

for jar in `find ${AUTO_CLASSPATH_DIR} -name "*.jar" 2>/dev/null`; do
    pathappend $jar CLASSPATH
done

export JAVA_HOME
unset AUTO_CLASSPATH_DIR dir jar

# End /etc/profile.d/openjdk.sh</literal>
EOF</userinput></screen>

    <para>
      Hvis <ulink url="&blfs-svn;/postlfs/sudo.html">Sudo</ulink> er
      installert, skal superbrukeren ha tilgang til variablene ovenfor. 
      Kjør følgende kommandoer som &root; bruker:
    </para>

<screen role="root"><userinput>cat &gt; /etc/sudoers.d/java &lt;&lt; "EOF"
<literal>Defaults env_keep += JAVA_HOME
Defaults env_keep += CLASSPATH</literal>
EOF</userinput></screen>

    <para>
      For å få <command>mandb</command> til å inkludere OpenJDK manualsidene i 
      databasen, utfør, som <systemitem class="username">root
      </systemitem> bruker:
    </para>

<screen role="root"><userinput>cat &gt;&gt; /etc/man_db.conf &lt;&lt; "EOF" &amp;&amp;
<literal># Begin Java addition
MANDATORY_MANPATH     /opt/jdk/man
MANPATH_MAP           /opt/jdk/bin     /opt/jdk/man
MANDB_MAP             /opt/jdk/man     /var/cache/man/jdk
# End Java addition</literal>
EOF

mkdir -p /var/cache/man &amp;&amp;
mandb -c /opt/jdk/man</userinput></screen>

    &source-profile;

  </sect2>

  <sect2 id="ojdk-certs" xreflabel="installere JVM sertifiseringsinstansens sertifikater">

    <title>Konfigurere sertifikatmyndighetens sertifikater for Java</title>

    <para>
      OpenJDK bruker sitt eget format for 
      CA sertifikater. Java sikkerhetsmodulene bruker
      <envar>$JAVA_HOME</envar><filename>/lib/security/cacerts</filename> som
      standard. For å ha alle sertifikatene på ett sted, bruker vi
      <filename>/etc/pki/tls/java/cacerts</filename>. Instruksjonene 
      på <ulink url="&blfs-svn;/postlfs/make-ca.html">make-ca</ulink> siden
      opprettet tidligere filen som ligger i <filename
      class="directory">/etc/pki/tls/java</filename>. Sett opp en symbollenke 
      på standardplasseringen som &root; bruker:
    </para>

<screen role="root"><userinput>ln -sfv /etc/pki/tls/java/cacerts /opt/jdk/lib/security/cacerts</userinput></screen>

    <para>
      Bruk følgende kommando for å sjekke om <filename>cacerts</filename>
      filen er installert:
    </para>

<screen role="root"><userinput>/opt/jdk/bin/keytool -list -cacerts</userinput></screen>

    <para>
      Ved ledeteksten <computeroutput>Enter keystore password:</computeroutput>,
      skriv <userinput>changeit</userinput> (standard) eller bare trykk på
      <quote>retur</quote> tasten. Hvis <filename>cacerts</filename> filen ble 
      installert riktig, vil du se en liste over sertifikatene med relatert 
      informasjon for hvert enkelt. Hvis ikke, må du installere dem på nytt.
    </para>

    <para>
      Hvis du senere installerer en ny JVM, trenger du bare å opprette 
      symbollinken på standardplasseringen for å kunne bruke cacertene.
    </para>

  </sect2>

</sect1>
